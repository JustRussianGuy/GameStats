{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/app/main.go",
            "env": {
                "swaggerPath": "${workspaceFolder}/api/swagger/gamestats.swagger.json",
                "configPath": "${workspaceFolder}/config.yaml"
            }
        }
    ]
}